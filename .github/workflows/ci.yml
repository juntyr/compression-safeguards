name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - "*"

env:
  CLICOLOR: 1

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6

      - name: Check with ruff
        uses: astral-sh/ruff-action@v3
        with:
          args: check

  spelling:
    name: Spellcheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6

      - name: Spellcheck repo
        uses: crate-ci/typos@v1.40.0

  fmt:
    name: Formatting
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6

      - name: Check with ruff
        uses: astral-sh/ruff-action@v3
        with:
          args: "format --diff --check"

  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Replace symlinks with file copies
        run: find ./ -type l -exec sh -c 'for i in "$@"; do cp --preserve --remove-destination "$(readlink -f "$i")" "$i"; done' sh {} +

      - name: Build all packages
        run: uv build --all-packages

      - name: List the built dist files
        run: ls -lh dist/

      - name: Upload the package sdist and wheels
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist
          retention-days: 1

  test:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: build
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13", "3.14"]
        resolution: ["highest", "lowest-direct"]
        markers: ["not slow and not veryslow", "slow", "veryslow"]
        exclude:
          - python: "3.11"
            markers: "veryslow"
            resolution: "highest"
          - python: "3.12"
            markers: "veryslow"
          - python: "3.13"
            markers: "veryslow"
          - python: "3.14"
            markers: "veryslow"
            resolution: "lowest-direct"
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            /pyproject.toml
            /tests/

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python }}

      - name: Download the dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Install the safeguards packages
        run: |
          uv pip install dist/*.whl \
            --resolution ${{ matrix.resolution }} \
            --no-build

      - name: Install the test dependencies
        run: |
          uv sync --only-group test \
            --resolution ${{ matrix.resolution }} \
            --no-build

      - name: Run tests
        run: uv run pytest -v -W error -m "${{ matrix.markers }}"

  typing:
    name: Typecheck
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: build
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13", "3.14"]
        resolution: ["highest", "lowest-direct"]
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            /pyproject.toml

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python }}

      - name: Download the dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Install the safeguards packages
        run: |
          uv pip install dist/*.whl \
            --resolution ${{ matrix.resolution }} \
            --no-build

      - name: Install the typecheck dependencies
        run: |
          uv sync --only-group typecheck \
            --resolution ${{ matrix.resolution }} \
            --no-build

      - name: Typecheck compression-safeguards
        run: uv run mypy -p compression_safeguards

      - name: Typecheck numcodecs-safeguards
        run: uv run mypy -p numcodecs_safeguards

      - name: Typecheck xarray-safeguards
        run: uv run mypy -p xarray_safeguards

  fuzz:
    name: Fuzz
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: write
    needs: build
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        python: ["3.11", "3.12", "3.13", "3.14"]
        resolution: ["highest", "lowest-direct"]
        exclude:
          - python: "3.12"
            resolution: "lowest-direct"
          - python: "3.13"
            resolution: "lowest-direct"
          - python: "3.14"
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            /fuzz/
            /pyproject.toml

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python }}

      - name: Download the dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Install the safeguards packages
        run: |
          uv pip install dist/*.whl \
            --resolution ${{ matrix.resolution }} \
            --no-build

      - name: Install the fuzzing dependencies
        run: |
          uv sync --only-group fuzz \
            --resolution ${{ matrix.resolution }} \
            --no-build

      - name: Restore the fuzzer corpus
        id: fuzzer-corpus-restore
        uses: actions/cache/restore@v5
        with:
          path: ./corpus
          key: fuzzer-corpus

      - name: Ensure the fuzzer corpus exists
        run: |
          mkdir -p ./corpus/safeguards
          mkdir -p ./corpus/intervals
          mkdir -p ./corpus/qois
          mkdir -p ./corpus/chunking

      - name: Run the fuzzers
        run: |
          uv run fuzz/fuzz_safeguards.py ./corpus/safeguards -max_total_time=60
          uv run fuzz/fuzz_intervals.py ./corpus/intervals -max_total_time=30
          uv run fuzz/fuzz_qois.py ./corpus/qois -max_total_time=60
          uv run fuzz/fuzz_chunking.py ./corpus/chunking -max_total_time=30

      - name: Delete the previous fuzzer corpus
        if: always() && steps.fuzzer-corpus-restore.outputs.cache-hit == 'true'
        run: gh cache delete fuzzer-corpus
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Save the fuzzer corpus
        if: always()
        uses: actions/cache/save@v5
        with:
          path: ./corpus
          key: fuzzer-corpus

  docs:
    name: Docs
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Setup the Python venv with the safeguards
        run: |
          uv sync --all-packages \
            --resolution highest \
            --no-build \
            --no-binary-package compression-safeguards \
            --no-binary-package numcodecs-safeguards \
            --no-binary-package xarray-safeguards

      - name: Install the mkdocs dependencies
        run: uv pip install -r docs/requirements.txt

      # https://github.com/mkdocstrings/python/issues/167
      - name: Reduce docs duplication
        run: sed -i 's/__all__ = \["Safeguards", "SafeguardKind"\]/__all__ = \[\]/g' src/compression_safeguards/__init__.py

      - name: Check docs build
        run: uv run mkdocs build

  doctest:
    name: Doctest
    runs-on: ubuntu-latest
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13", "3.14"]
        resolution: ["highest", "lowest-direct"]
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.python }}

      - name: Setup the Python venv with the safeguards
        run: |
          uv sync --all-packages \
            --group doctest \
            --resolution ${{ matrix.resolution }} \
            --no-build \
            --no-binary-package compression-safeguards \
            --no-binary-package numcodecs-safeguards \
            --no-binary-package xarray-safeguards

      - name: Compile all docs
        run: |
          cp README.md docs/
          uv run griffe2md src/compression_safeguards/ -o docs/compression_safeguards.md
          uv run griffe2md packages/numcodecs-safeguards/src/numcodecs_safeguards/ -o docs/numcodecs_safeguards.md
          uv run griffe2md packages/xarray-safeguards/src/xarray_safeguards/ -o docs/xarray_safeguards.md
          find docs -type f -name "*.md" | xargs sed -i -e 's/```py/```python/g'

      - name: Run doctests
        run: uv run pytest -v -W error --codeblocks docs/

  pyodide-build:
    name: Build for Pyodide
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            /tools/

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: 3.13.2

      - name: Setup Emscripten
        uses: mymindstorm/setup-emsdk@v14
        with:
          version: 4.0.9

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install pyodide-build
        run: uv pip install pyodide-build

      - name: Install the Pyodide build env
        run: uv run pyodide xbuildenv install 0.29.0

      - name: Build compiled dependencies for Pyodide
        run: |
          uv run pyodide build-recipes \
            --recipe-dir tools/ci/pyodide-recipes/packages \
            --install \
            google-crc32c numcodecs numpy numpy-quaddtype

      - name: Clean the Pyodide dist
        run: find dist/ -type f ! -name '*.whl' -delete

      - name: Upload the Pyodide wheels
        uses: actions/upload-artifact@v4
        with:
          name: pyodide-dist
          path: dist
          retention-days: 1

  pyodide-test:
    name: Test on Pyodide
    runs-on: ubuntu-latest
    permissions:
      contents: read
    needs: [build, pyodide-build]
    strategy:
      fail-fast: false
      matrix:
        markers: ["not slow and not veryslow", "slow", "veryslow"]
    steps:
      - name: Checkout the Repository
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            /pyproject.toml
            /tests/

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: 3.13.2

      - name: Install pyodide-build
        run: uv pip install pyodide-build

      - name: Install the Pyodide build env
        run: uv run pyodide xbuildenv install 0.29.0

      - name: Download the dist artifact
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Download the pyodide-dist artifact
        uses: actions/download-artifact@v4
        with:
          name: pyodide-dist
          path: dist

      - name: Create the Pyodide venv
        run: |
          # Set up Pyodide venv
          uv run pyodide venv .venv-pyodide
          source .venv-pyodide/bin/activate
          # Install pre-built packages
          python -m pip install $(ls dist/*.whl)
          # Install test dependencies
          python -m pip install --only-group test

      - name: Run tests
        run: |
          source .venv-pyodide/bin/activate
          python -m pytest -v -W error -m "${{ matrix.markers }}"
